spring:
  datasource:
    url: jdbc:mysql://localhost:3306/chronos?serverTimezone=Asia/Seoul&characterEncoding=utf8
    username: ehy0419
    password: 940419
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # ?? ? ??. ??? validate ??.
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false

server:
  port: 8080
